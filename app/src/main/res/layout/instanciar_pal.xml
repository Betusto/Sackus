<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="100dp"


    android:orientation="horizontal" android:focusable="true" android:focusableInTouchMode="true" >
    <EditText
        android:id="@+id/instanciar_pal_amigo_edittext"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:inputType="textCapSentences"
        android:layout_weight="5"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="10dp"
        android:hint="Nombre de usuario del acompañante"
        android:singleLine="true"/>

    <Button
        android:id="@+id/instanciar_pal_eliminar_button"
        android:layout_width="0dp"
        android:layout_height="45dp"
        android:layout_weight="0.6"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="10dp"
        android:background="@mipmap/ic_eliminar"
        android:onClick="onDelete" />
</LinearLayout>